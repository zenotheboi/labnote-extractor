# ── Core (always required) ────────────────────────────────────────────────
jsonschema>=4.0
pytest>=7.4
anthropic>=0.40              # whole-page VLM recogniser

# ── Classical CV  (preprocess, layout) ────────────────────────────────────
numpy>=1.24
pillow>=10.0
opencv-python-headless>=4.8

# ── Handwriting OCR (ocr_text) ────────────────────────────────────────────
# transformers>=4.35          # TrOCR
# torch>=2.0
# azure-cognitiveservices-vision-computervision  # Azure Read API

# ── Math OCR (ocr_math) ───────────────────────────────────────────────────
# pix2tex>=0.1.1              # LaTeX OCR (pix2tex)

# ── OCSR — chemical structure recognition (ocsr) ──────────────────────────
rdkit>=2023.9                 # SMILES validity gate, canonicalisation, Tanimoto
# DECIMER: install separately — heavy TensorFlow deps, see README. ocsr.py uses
# it when present; otherwise the recogniser's drawn SMILES is RDKit-gated instead.
#   pip install decimer       # (recommend a separate venv — numpy/TF pins)
# MolScribe / MolNexTR: see README for install instructions

# ── Name resolution (chem_resolve) ────────────────────────────────────────
py2opsin>=1.2                 # OPSIN IUPAC→SMILES wrapper (requires a JRE)
requests>=2.28                # PubChem REST API (primary name→SMILES resolution)

# ── Optional ──────────────────────────────────────────────────────────────
# pint>=0.22                  # unit handling / dimensional analysis
